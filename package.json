{
  "name": "patient-registration",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd patient-registration-app && npm install && npm run build",
    "vercel-build": "cd patient-registration-app && npm install && npm run build"
  }
} 